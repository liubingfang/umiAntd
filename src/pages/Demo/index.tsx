import { PageContainer } from '@ant-design/pro-layout';
import React from 'react';

const Demo = (): React.ReactElement => {
  return <PageContainer title={false}>Demo</PageContainer>;
};

export default Demo;
